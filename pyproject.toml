[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
boto3 = "^1.34.134"
botocore = "^1.34.134"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
